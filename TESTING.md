# 校园反诈卫士 - 自动化测试方案

## 项目概述
本项目是一款以"守护校园安全，远离网络诈骗"为目标的微信小程序，集科普、预警、查询、互动于一体，帮助大学生识别诈骗陷阱、掌握防范技巧。

## 测试目标
- 验证所有功能模块的正确性
- 确保交互流程的顺畅性
- 检测潜在的性能问题
- 保证在不同场景下的稳定性

---

## 测试模块划分

### 1. 首页功能测试 (Index Page)
#### 测试点：
- ✅ 轮播图展示与点击跳转
- ✅ 快速入口导航
- ✅ 搜索功能及关键词预警
- ✅ 热门案例和最新指南展示
- ✅ 一键报警功能
- ✅ 首次进入欢迎弹窗

#### 预期结果：
- 轮播图自动轮播，点击可跳转对应页面
- 快速入口可正常跳转至对应模块
- 搜索框输入"刷单"、"校园贷"等关键词应触发预警弹窗
- 热门案例和指南按预期展示
- 点击一键报警应弹出确认对话框

---

### 2. 案例库功能测试 (Cases Page)
#### 测试点：
- ✅ 案例分类展示
- ✅ 案例列表加载
- ✅ 案例详情查看
- ✅ 视频组件展示
- ✅ 搜索功能

#### 预期结果：
- 6类诈骗案例应正确分类展示
- 点击案例可进入详情页
- 案例详情页包含案情描述、诈骗手段分析、防范要点
- 视频组件正常渲染（如配置了视频URL）

---

### 3. 防骗指南功能测试 (Guide Page)
#### 测试点：
- ✅ 指南分类展示
- ✅ 指南搜索功能
- ✅ 指南详情查看
- ✅ 关键词搜索匹配

#### 预期结果：
- 指南列表按分类正确展示
- 搜索功能可根据关键词筛选对应指南
- 指南详情页内容完整展示

---

### 4. 知识测试功能测试 (Quiz Page)
#### 测试点：
- ✅ 登录状态验证
- ✅ 分类选择
- ✅ 题目随机抽取
- ✅ 答题流程（单选、多选、判断）
- ✅ 答案提交与反馈
- ✅ 结果统计
- ✅ 测试记录保存

#### 预期结果：
- 未登录用户应提示登录
- 登录用户可选择分类开始测试
- 题目类型正确呈现（单选/多选/判断）
- 提交答案后显示正确/错误反馈
- 测试完成后显示得分和错题解析
- 测试历史记录应保存到本地存储

---

### 5. 求助通道功能测试 (Mine Page)
#### 测试点：
- ✅ 求助热线展示
- ✅ 一键拨号功能
- ✅ 地图组件展示
- ✅ 用户登录状态显示

#### 预期结果：
- 求助热线号码正确显示
- 点击电话可触发拨号功能
- 地图组件正常显示位置
- 登录状态下显示用户信息

---

### 6. 登录功能测试 (Login Page)
#### 测试点：
- ✅ 微信快捷登录
- ✅ 游客模式选择
- ✅ 登录状态持久化
- ✅ 权限控制

#### 预期结果：
- 微信快捷登录后保存登录状态
- 游客模式可浏览基础功能
- 未登录用户无法使用知识测试和求助详情

---

## 测试用例设计

### 用例1：首页搜索预警测试
**场景**：用户在首页搜索框输入敏感关键词
**步骤**：
1. 进入首页
2. 在搜索框输入"刷单"
3. 观察是否触发预警弹窗
**预期结果**：应显示刷单诈骗的预警提示弹窗

### 用例2：知识测试流程测试
**场景**：已登录用户进行反诈知识测试
**步骤**：
1. 登录成功后进入测试页面
2. 选择分类并开始测试
3. 连续作答5道题
4. 提交所有答案
5. 查看测试结果
**预期结果**：
- 题目按分类正确抽取
- 答题过程流畅
- 结果页面显示正确得分和错题解析

### 用例3：一键报警测试
**场景**：用户在首页点击一键报警
**步骤**：
1. 进入首页
2. 点击"一键报警"按钮
3. 在确认对话框中点击"拨打"
**预期结果**：应触发拨号功能，拨打110

### 用例4：权限控制测试
**场景**：未登录用户尝试访问受限功能
**步骤**：
1. 以游客模式进入应用
2. 点击"知识测试"入口
3. 点击"求助详情"入口
**预期结果**：应提示需要登录才能使用该功能

---

## 自动化测试实现

### 测试框架选择
- **微信小程序自动化测试框架**：miniprogram-automator
- **优势**：官方支持，稳定可靠，可与微信开发者工具集成

### 核心测试代码结构

```javascript
// test/index.test.js
const automator = require('miniprogram-automator');

describe('首页功能测试', () => {
  let miniProgram;
  let page;

  beforeAll(async () => {
    miniProgram = await automator.launch({
      cliPath: '/Applications/wechatwebdevtools.app/Contents/MacOS/cli',
      projectPath: '/path/to/your/project'
    });
    page = await miniProgram.reLaunch('/pages/index/index');
    await page.waitFor(500);
  });

  afterAll(async () => {
    await miniProgram.close();
  });

  test('首页轮播图展示', async () => {
    const swiper = await page.$('.swiper-container');
    expect(swiper).not.toBeNull();

    const slides = await swiper.$$('.swiper-slide');
    expect(slides.length).toBe(3);
  });

  test('搜索关键词触发预警', async () => {
    const searchInput = await page.$('.search-input');
    await searchInput.input('刷单');

    const warningModal = await page.$('.warning-modal');
    expect(warningModal).not.toBeNull();

    const modalContent = await warningModal.$('.modal-content');
    const contentText = await modalContent.text();
    expect(contentText).toContain('警惕刷单诈骗');
  });
});
```

### 测试运行方法

1. **安装依赖**：
```bash
npm install miniprogram-automator --save-dev
```

2. **配置测试脚本**：
在 `package.json` 中添加：
```json
{
  "scripts": {
    "test": "jest test/**/*.test.js"
  }
}
```

3. **运行测试**：
```bash
npm run test
```

---

## 性能测试要点

1. **页面加载时间**：每个页面加载时间应小于2秒
2. **图片加载**：所有图片资源应正常加载，无404错误
3. **数据处理**：搜索和分类筛选响应时间应小于500ms
4. **本地存储**：数据读写操作应流畅无卡顿

---

## 兼容性测试

1. **微信版本**：支持微信7.0及以上版本
2. **操作系统**：iOS和Android平台均需测试
3. **屏幕尺寸**：适配不同屏幕尺寸的手机

---

## 测试报告模板

```
# 测试报告 - 校园反诈卫士小程序

## 基本信息
- 测试日期：YYYY-MM-DD
- 测试版本：v1.0.0
- 测试人员：自动化测试

## 测试结果概览
- 总测试用例数：XX
- 通过：XX
- 失败：XX
- 通过率：XX%

## 详细测试结果
| 测试模块 | 用例数 | 通过数 | 失败数 | 备注 |
|---------|--------|--------|--------|------|
| 首页功能 | XX | XX | XX | - |
| 案例库功能 | XX | XX | XX | - |
| 防骗指南功能 | XX | XX | XX | - |
| 知识测试功能 | XX | XX | XX | - |
| 求助通道功能 | XX | XX | XX | - |
| 登录功能 | XX | XX | XX | - |

## 失败用例详情
1. [模块名] - [用例名]
   - 失败原因：...
   - 截图/日志：...

## 性能指标
- 首页加载时间：XX ms
- 搜索响应时间：XX ms
- 测试流程耗时：XX ms

## 结论
测试是否通过：是/否
需要修复的问题：
- 问题1：...
- 问题2：...
```

---

## 持续集成建议

1. **自动化测试流水线**：
   - 每次代码提交自动运行测试
   - 测试通过才能合并代码

2. **测试环境隔离**：
   - 开发环境、测试环境、生产环境分离
   - 测试数据与生产数据隔离

3. **测试覆盖率**：
   - 核心功能模块测试覆盖率达到80%以上
   - 定期分析测试覆盖率报告

---

## 维护与更新

- 每次功能迭代后更新相应测试用例
- 定期检查测试脚本的有效性
- 根据用户反馈补充测试场景
- 优化测试用例的执行效率
